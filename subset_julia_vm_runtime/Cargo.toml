[package]
name = "subset_julia_vm_runtime"
version = "0.1.0"
edition = "2021"
description = "Runtime library for SubsetJuliaVM AoT compiled code"
license = "MIT"

[dependencies]
thiserror = "1.0"

[features]
default = []
