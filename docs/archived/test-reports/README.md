# Archived Feature Test Reports

This directory stores dated feature-test snapshots. Treat these files as
historical reports from the timestamp in each filename, not as current product
or CLI documentation.

For current instructions, use the active documents under `docs/aot/`,
`docs/vm/`, and the repository README.
