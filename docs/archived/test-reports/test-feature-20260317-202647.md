# Feature Test Report: SubsetJuliaVM

**Date:** 2026-03-17 20:26:47 JST
**Project type:** Mixed Rust CLI + Swift/iOS + WASM workspace
**Features tested:** `sjulia --precompile-base` workflow (re-test after docs fix)
**Profile:** ephemeral
**Use Case:** A CLI user follows the updated README two-pass build workflow from the repository root to generate and embed the Base cache.
**Expected Outcome:** The updated README should be sufficient to run the cache-generation step and the rebuild step without extra undocumented path fixes.
**Verdict:** pass
**Critical Issues:** 0

## Summary

| Feature | Discoverable | Setup | Works | Expected Outcome Met | Doc Quality |
|---------|-------------|-------|-------|---------------------|-------------|
| `sjulia --precompile-base` workflow | yes | yes | yes | yes | good |

## Per-Feature Details

### `sjulia --precompile-base`
- **Role:** Intermediate Julia/Rust CLI user following the updated README
- **Use Case:** Generate `target/base_cache.bin` and rebuild `sjulia` with `SJULIA_BASE_CACHE`
- **What they tried:** Ran `mkdir -p target`, `./target/release/sjulia --precompile-base target/base_cache.bin`, then `SJULIA_BASE_CACHE=$(pwd)/target/base_cache.bin cargo build --release --bin sjulia --features repl`
- **Discoverability:** The updated README makes the repository-root and workspace-`target/` assumption explicit
- **Setup:** Worked as documented
- **Functionality:** Cache generation succeeded and the rebuild completed successfully
- **Expected vs Actual Outcome:** The updated instructions matched actual behavior
- **Blocked steps:** None
- **Friction points:** Existing compiler warnings still appear during build, but they did not block the workflow
- **Doc suggestions:** None for this path after the README update

## Expected vs Actual Outcome

- **Expected:** The revised two-pass build instructions should run as written from the repository root
- **Actual:** The workflow completed successfully with the documented commands and produced `target/base_cache.bin`

## Issues Found

1. No blocking issues found in the re-tested path

## Suggestions

1. Optional: reduce or document the existing compiler warnings so tutorial-style output is cleaner

## Commands Run

```bash
mkdir -p target
./target/release/sjulia --precompile-base target/base_cache.bin
SJULIA_BASE_CACHE=$(pwd)/target/base_cache.bin cargo build --release --bin sjulia --features repl
```
