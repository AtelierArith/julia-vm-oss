[package]
name = "subset_julia_vm_parser_common"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "Canonical CST shared between the legacy and lezer-compatible parsers (Issue #11049)"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
